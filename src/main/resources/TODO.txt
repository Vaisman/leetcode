Question:
- goole doc formating
- mock interview

Phone:
- google doc template with solution/samples/tests
  Tools -> Preference, uncheck all the boxes.
  Change the font to Consolas (optional;personal preference)
  Tab in two symbols

On cite:
- plugin for google doc
- fresh marker
- water

- Describe yourself and what you did


int[] hash = new int[255];  // hash

if (x==null || y==null)
   return x==y;

count.put(x, count.getOrDefault(x, 0) + 1);

Collections.max(count.values());

for (int x: count.keySet())

res.add(Arrays.asList(num[i], num[j], num[k]));

gray i ^ i/2

for (int x = Math.max(i - 1, 0); x <= Math.min(i + 1, m - 1); x++) {
  for (int y = Math.max(j - 1, 0); y <= Math.min(j + 1, n - 1); y++)

 element -> greaterElement in
 |
 | for (int num : nums) {
 |     while (!stack.isEmpty() && stack.peek() < num)
 |         map.put(stack.pop(), num);
 |
 |     stack.push(num);
 | }
